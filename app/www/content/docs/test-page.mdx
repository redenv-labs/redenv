---
title: UI Test Page
description: A page to test all documentation UI elements and typography.
---

# Top Level Heading

This is a paragraph of text to test the body typography. It should be legible, have appropriate line height, and contrast. **Bold text** and _italic text_ should be clearly distinguishable.

## Second Level Heading

Here is an unordered list:

- Item one
- Item two
- Item three with a [link](#)

Here is an ordered list:

1. First step
2. Second step
3. Third step

### Third Level Heading

Let's test some code blocks:

```tsx title="example.tsx"
import * as React from "react";

export function Component() {
  return <div>Hello World</div>;
}
```

```bash title="Terminal"
pnpm install @redenv/cli
```

## UI Components

### Callouts

<Callout title="Tip">
    It can be used without Fumadocs UI, in other words, it's headless.

    For beginners and normal usages, use [Fumadocs UI](/docs/ui).

</Callout>

<Callout title="Warning" type="warn">
  This is a warning callout. Watch out!
</Callout>

<Callout title="Error" type="error">
  This is an error callout. Something went wrong.
</Callout>

### Tabs

<Tabs items={["npm", "pnpm", "yarn"]}>
  <Tab value="npm">npm install package</Tab>
  <Tab value="pnpm">pnpm add package</Tab>
  <Tab value="yarn">yarn add package</Tab>
</Tabs>

### Install Tabs (Custom Block)


```npm
npm i fumadocs-ui fumadocs-core
```

```npm
npm i fumadocs-ui fumadocs-core
```


### Steps

<Steps>
  <Step>
    <h4>Install the CLI</h4>
    <p>Run the installation command in your terminal.</p>
  </Step>
  <Step>
    <h4>Login</h4>
    <p>Authenticate with your account.</p>
  </Step>
  <Step>
    <h4>Login</h4>
    <p>Authenticate with your account.</p>
  </Step>
</Steps>

## Table

| Feature        | Support | specifics        |
| :------------- | :------ | :--------------- |
| Encryption     | ✅      | AES-256-GCM      |
| Zero Knowledge | ✅      | Client-side only |
| Free Tier      | ✅      | Generous limits  |
